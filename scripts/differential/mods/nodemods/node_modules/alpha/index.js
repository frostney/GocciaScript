// Main entry selected through the "." export's "import" condition. Imports a
// bare dependency of its own, so resolution has to work from inside a package.
import { betaLabel } from "beta";

export const alphaLabel = "alpha";
export const chain = () => `${alphaLabel}>${betaLabel()}`;
