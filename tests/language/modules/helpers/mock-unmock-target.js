// Target for the "mock then unmock, last one wins" direction of the
// source-order rule. Kept separate so the two orderings can be exercised in
// the same test file without fighting over one address.
export const label = "REAL-UNMOCKED";
