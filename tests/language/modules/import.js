// TODO: Module import path resolution is relative to CWD, not the importing file.
// Once LoadModule resolves paths relative to the importing file's directory,
// uncomment these tests.
// import { localFunction } from "./local-module.js";

// test("import", () => {
//   expect(localFunction()).toBe(42);
// });
