// Sits outside every fixture package, beside node_modules rather than in it.
// A resolution that reaches this file means the package boundary leaked, so
// the refusal tests aim every escape route here.
export const escaped = "ESCAPED";
