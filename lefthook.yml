glob_matcher: doublestar

pre-commit:
  commands:
    format-pascal:
      glob: "**/*.{pas,dpr}"
      run: ./format.pas {staged_files}
      stage_fixed: true
    check-swimmies:
      glob: "**/*.{pas,dpr}"
      run: bun check-swimmies.ts {staged_files}
